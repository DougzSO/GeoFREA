# F1b data_quality_audit

Status: `built_pending_conformance`
Methodology items: M-F1b-01, M-F1b-02, V-04

## Contract

Requires: `acquisition_registry`. Produces: `audit_report` (per-layer summaries, anomalies). Blocks nothing.

## Conformance

| Item | Requirement | Current state | Status |
|---|---|---|---|
| M-F1b-01 | Expected resolutions and sanity ranges in configuration | Moved to `config/audit.yaml` (`AuditConfig`), read by `audit.py` via `run_audit_phase(..., audit_config=...)`. Every value carries a `source` or is `null` with an OQ ID (F7-2). | pass |
| M-F1b-01 | PVOUT unit reported as kWh/kWp/day | Fixed: `config/audit.yaml`'s `solar.unit` is `kWh/kWp/day`, confirmed against the file's own XML sidecar (`gmd:abstract`, `gmd:RecordType`, both state "PVOUT ... in kWh/kWp") — the prior "kWh/m2/day" label was a code error, not a METHODOLOGY divergence (F7-2 action 3). | pass |
| M-F1b-01 | Wind expected resolution matches GWA native grid | Resolved for all four GWA products, all three countries (2026-09-23, task F1-2): 0.0025 deg, confirmed from native GDAL metadata of BRA/PRT/IND `wind-speed`, `combined-Weibull-A`, `combined-Weibull-k` and `air-density` files, all twelve agreeing — see `config/audit.yaml`. Resolves OQ-027. | pass |
| M-F1b-01 | Audit covers new layers (Weibull, air density, CMIP6, ERA5, GEM) | `combined-Weibull-A`/`combined-Weibull-k`/`air-density` moved from `not_audited` to real raster inspection 2026-09-23 (task F1-2): `AuditInputs` gained `weibull_a_path`/`weibull_k_path`/`air_density_path`, `audit.py`'s `_UNACQUIRED_GWA_PRODUCTS` is now empty. `cmip6`/`era5_gust`/`gem_existing_plants` remain `not_audited` with a reason and task ID (F-3/F-4/F-5) — not yet acquired. | partial |
| M-F1b-02 | Reports, never blocks | Verified: `data_quality_audit/audit.py:run_audit_phase` contains no `raise` statement; `AuditInputs` (schemas.py) defaults every field to `None`, degrading to "file not found" reporting rather than failing. A layer with no configured expectation degrades to `not_audited`, same non-blocking pattern. | pass |
| A-02 (vector clip path) | Invalid-geometry repair unconditional for every clip-path caller, not just suitability_criteria | `inspect_vector_layer()`'s clip path (`vector_inspection.py::_read_clipped_with_cache` -> `core/geo_utils.py::read_clipped_to_country`/`clip_vector_to_country`) now repairs before intersecting, same as every other caller — see docs/phases/core.md D-core-003. `VectorLayerInspection.geometry_repair` reports the count. | pass |

## Active implementation decisions

- **D-F1b-006 — CRS assigned to Weibull-A/k from an exact-match reference, at read time only; grid identity confirmed across all 36 product/height/country combinations (task F1-3, 2026-09-23).** Follow-up to D-F1b-005 below, which found the gap but deliberately did not fix it. Two things verified before any code changed, per file metadata, not assumption:
  1. **Resolution/dimensions (M-F1-03, OQ-027):** for all 4 products x 3 heights x 3 countries (36 files), `rasterio` reports identical pixel size (0.0025 deg), width, height and bounds per country, regardless of product or height — the file-size difference (PRT: 4.5-4.7 MB for `air-density` vs 14.7-16.8 MB for the other three, same extent) is compression only (a smoother field compresses harder under the shared ZSTD/COG layout), not a coarser grid. OQ-027 stays closed on the single 0.0025 deg figure; `config/audit.yaml` is unchanged by this finding.
  2. **CRS identity:** `combined-Weibull-A`/`combined-Weibull-k` (no embedded CRS) were compared field-by-field against `wind-speed` (EPSG:4326) at the same country and height — `transform`, `bounds` and `(width, height)` all compare exactly equal (`==`, not within-tolerance) for all 18 pairs (2 products x 3 heights x 3 countries). Assigning EPSG:4326 to the two CRS-less products is therefore an observed fact, not an inference.

  Implementation, in exactly one place: `raster_inspection.py::_effective_crs()` (new), called from `inspect_raster()` right after opening the file. If the file has a CRS, that CRS is used unchanged. If not, and a reference path was given (`inspect_raster(..., assume_crs_from=...)`), the reference is opened and its `transform`/`bounds`/`(width, height)` are compared to the target; only on an exact match is the reference's CRS borrowed, purely in-memory (`geopandas.to_crs()` calls inside `_mask_raster_by_polygon()`/`_stats_chunked()`, both now taking an `effective_crs` parameter) — the downloaded file itself is never opened for writing, so its `source_sha256` (computed at fetch time, D-core-016) stays meaningful. No reference given, or the reference disagrees (`CrsAssumptionMismatchError`), or the file has a CRS to begin with is not needed (`MissingCrsWithNoReferenceError`) — every other case fails loud, not a silent default. `audit.py`'s `raster_map` loop passes `assume_crs_from=inputs.wind_paths[0]` for `weibull_a`/`weibull_k` only (`_crs_reference_by_layer`), since that's the pairing verified above; no other layer requests this.

  Real result, BRA and PRT: `weibull_a`/`weibull_k` now inspect cleanly (`crs=EPSG:4326`, real stats) — Weibull A means ~4.3-6.4 m/s, Weibull k means ~2.0-2.3 (physically plausible wind-resource shape parameters), air-density means ~1.12-1.17 kg/m3 (physically plausible at these countries' elevations). `not_audited` stays free of every `wind/*` entry, same as before this fix; the earlier `[MISSING] ... Must pass either crs or epsg.` finding no longer appears.
- **D-F1b-005 — Weibull-A/k, air-density wired into raster inspection; a real CRS-metadata gap found in two of the four GWA products (task F1-2, 2026-09-23; CRS gap resolved by D-F1b-006 above).** `raster_map` (`audit.py`) gained `weibull_a`/`weibull_k`/`air_density` (from `AuditInputs`' new fields, each the product's 100m file — representative, same "inspect one file" precedent as `wind`); `expected_resolutions` reads their `config/audit.yaml` entries via the new `_GWA_PRODUCT_RASTER_KEYS` mapping. Real run, BRA and PRT (IND blocked, see Known issues): `wind` and `air_density` inspect cleanly; `weibull_a` and `weibull_k` both fail with `Must pass either crs or epsg.` — confirmed via direct `rasterio` inspection (`ds.crs is None`, no GCPs, no tags) that the GWA CDN genuinely serves these two products with no embedded CRS at all, unlike `wind-speed`/`air-density` (both EPSG:4326). `[MISSING] WEIBULL_A/WEIBULL_K: Must pass either crs or epsg.` was the correct, honest report at the time (M-F1b-02: reports, never blocks — `data_quality_audit` still completed `status=success` for both countries).
- **D-F1b-004 — slope removed from config/audit.yaml; F1b never audits it (resolves OQ-030).** Applied the "remove" option, not the "give it a distinct report status" option: slope is not produced until `grid_alignment` derives it from the DEM (`raster_alignment.derive_slope_from_dem()`), so F1b has no file to hold an expectation about in the first place — a permanently-null config entry misstated this as an open sourcing question when the real fact is a phase-ownership one. `AuditInputs.slope_path` stays in the schema (still reports `[MISSING] SLOPE: File not found` today, a status distinct from `not_audited` since the resolution check never runs for a layer whose inspection already errored) — only the `config/audit.yaml` entry and its OQ-030 backing are gone. See docs/phases/F2a_grid_alignment.md Known issues for where the expectation now belongs. Caveat: if `slope_path` were ever populated (AuditInputs' own docstring calls this "usually None", not "never"), `diagnose_consistency()` would silently skip its resolution check — same class of silent gap already tracked for `population` above, not a new one introduced here.
- **D-F1b-003 — land_cover tile cache: sha256 content check added; hit condition and its remaining gap recorded, OQ-026 extended.** Cache mechanics as of `raster_inspection.py` (`_land_cover_tile_cache_key`, `_load_land_cover_tile_cache`/`_save_land_cover_tile_cache`, called from `inspect_land_cover_tiles()`; wired in `audit.py`'s `land_cover_cache_dir`): one JSON file per tile at `outputs/<country>/data_quality_audit/land_cover_tile_cache/<tile_stem>.json`, holding that tile's CRS, resolution and per-class area contribution. A hit requires the tile's mtime, size, and sha256 of its own bytes, plus a sha256 of the masking country polygon's WKB, to all match a `cache_key` string stored alongside the payload — not path existence alone. Added 2026-09-23: the sha256 of the tile's bytes (`_file_sha256()`), closing the gap where the first version (2026-09-22, mtime+size only) could not tell a same-size/same-mtime content change (e.g. a tile silently re-downloaded or restored with a copied timestamp) from an unchanged tile. The polygon fingerprint already covered a changed island-filtering rule or a different `country_gdf`. Remaining, real gap: the key carries no version of the per-tile computation algorithm itself — if `inspect_land_cover_tiles()`'s class-area logic changes (a bug fix, a different windowing strategy), an old cache entry computed under the prior logic is silently reused forever, since nothing about the code version is in the key. Added as a second instance to OQ-026 (`docs/OPEN_QUESTIONS.md`), alongside the grid_alignment mosaic/interim caches — no key design proposed here, per that OQ's resolution protocol. A change to `excluded_land_cover_tiles` does NOT create wrong results for tiles that get processed (it only changes which tiles are even in the list passed in; a tile's own cached result doesn't depend on which other tiles were excluded) — this is not a vulnerability of this cache, unlike the algorithm-version gap above.
- **D-F1b-002 — M-F1b-01 expectations moved to config/audit.yaml, sourced from native file metadata.** `audit.py`'s five hardcoded literals (`_EXPECTED_RESOLUTIONS_DEG`, `_RESOLUTION_TOLERANCE`, `_SOLAR_PVOUT_SANITY_RANGE`) are gone; `run_audit_phase()` now takes an `AuditConfig` (loaded from `config/audit.yaml`) and `diagnose_consistency()` returns `(alerts, not_audited)`. Every configured value's `source` is either a primary reference or the layer is `null` with an OQ ID (OQ-027 to OQ-030) — resolved by reading GDAL metadata directly off the BRA/PRT files on disk (land_cover 0.0000833 deg, solar 0.0083333 deg, elevation 0.005 deg, wind-speed 0.0025 deg — all agreeing across both countries). PVOUT's unit was corrected to `kWh/kWp/day` (file's own XML metadata; matches M-F1-02 — the divergence was in the code, not METHODOLOGY). `slope` has no entry at all (see D-F1b-004: it's a grid_alignment-derived product, never an F1b input — removed, not left null). Layers with no fetch path at all (3 GWA products, CMIP6, ERA5, GEM) are now reported `not_audited` unconditionally instead of omitted. `AuditResult` gained `not_audited: dict[str, str]`; `AuditSummary` gained `n_not_audited`.
- **D-F1b-001 — Vector inspection distinguishes read_error from processing_error; WDPA repair is no longer audit-specific.** `inspect_vector_layer()` used to swallow both a file-open/clip failure and a downstream statistics failure into one `error` field, labeled "found (unreadable)" — indistinguishable without reading logs. Two try/except stages now set `error_type` (`"read_error"` | `"processing_error"`), and `VectorLayerSummary.status` mirrors that split in the audit report footer. Separately, the `[ERROR] found (unreadable)` PRT/protected case (`TopologyException: side location conflict`, confirmed pre-existing against unmodified commit `e1a57ed`, see History) was resolved as a side effect of moving invalid-geometry repair into the shared `clip_vector_to_country()` — not touched here directly. Real PRT run 2026-09-21: 14/184 WDPA features repaired (`Too few points in geometry component` x7, `Self-intersection` x5, `Ring Self-intersection` x2), `protected` now reports `[OK] found`.

## Known issues

- `audit_report`'s artifact `schema_version` was bumped to `"2.1"` 2026-09-22 (main.py `_AUDIT_REPORT_SCHEMA_VERSION`) when `AuditResult` gained the required `not_audited` field (D-F1b-002) — a manifest entry recorded under `"2.0"` or earlier now correctly raises `StaleManifestEntryError` on resume instead of a raw Pydantic `ValidationError`.
- `population` has no configured `expected_resolution_deg` in `config/audit.yaml` and is not in the `not_audited` set either — it was never checked before D-F1b-002 (no literal existed for it in the old `_EXPECTED_RESOLUTIONS_DEG`) and F7-1/F7-2 did not extend scope to it. M-F1b-01 technically requires it; tracked here as a gap, not an OQ, since no research is needed — it just needs a metadata read like the other four.
- Land-cover inspection for BRA takes about 37-55 minutes **on a cold or invalidated cache**; about 1 minute once the per-tile cache (D-F1b-003) is warm. Per-tile caching implemented 2026-09-22, hardened 2026-09-23. A run interrupted partway (real incident, 2026-09-22: a computer suspend killed a BRA run near tile 60/112) now resumes from the last cached tile instead of restarting from zero.
- **BRA F1b total runtime, corrected (task F1-3, 2026-09-23) — a prior report conflated numbers from two different runs.** The F1-2 report stated "BRA F1b runtime 4148.7s (land_cover cache reused, 56.7s not 55 min)" — that combination never happened in one run; it wrongly paired the TOTAL of one run with the land_cover time of a later, different run. The real, separately-sourced numbers (both saved reports, `outputs/BRA/audit/`): (1) `audit_BRA_20260923T201741.txt`, the run right after the M-F1-03 acquisition rerun (fresh land_cover files on disk, invalidating the tile-content-hash cache from that morning — confirmed: all 111 cache JSON files were rewritten within this run's window) — **TOTAL 4148.7s**, `land_cover` alone **3654.5s** (88.1%), the rest split `population` 194.3s, `lakes` 74.1s, `rivers` 56.1s, `roads` 53.7s, `wind` 29.9s, `protected` 20.1s, `grid` 18.1s, `elevation` 17.5s, `admin1` 15.8s, `borders` 8.0s, `solar` 5.9s, `power_plants` 0.3s. (2) `audit_BRA_20260923T213420.txt`, the very next run, cache now warm — **TOTAL 502.0s**, `land_cover` **56.8s**. (3) `audit_BRA_20260923T220716.txt`, after D-F1b-006's CRS fix, land_cover cache still warm — **TOTAL 511.8s**, `land_cover` **60.6s**, `weibull_a`/`weibull_k`/`air_density` now real inspections at ~21-22s each (previously 0.0s, since they errored immediately). PRT: `audit_PRT_20260923T214354.txt` **TOTAL 146.9s** (`land_cover` 95.0s, still partly cold for PRT's own 11-tile set); `audit_PRT_20260923T221555.txt` (CRS fix, fully warm) **TOTAL 49.3s** (`land_cover` 2.9s). No PRT report exists paired with BRA's 4148.7s run — that invocation's script crashed after BRA (a bug in the reporting script, not the audit) before reaching PRT.
- `audit_report`'s artifact `schema_version` was bumped to `"2.0"` 2026-09-21 (main.py `_AUDIT_REPORT_SCHEMA_VERSION`) when `VectorLayerSummary.status`'s literal set changed — a manifest entry recorded under `"1.0"` now correctly raises `StaleManifestEntryError` on resume (METHODOLOGY A-02/A-09, docs/phases/core.md) instead of a raw Pydantic `ValidationError`. Any future breaking change to `AuditResult`'s shape needs the same bump.
- The 2026-09-22 FD4c `force_rerun` audited the corrected, geometry-filtered `land_cover` registry (BRA 112/155 tiles, PRT 11/26 tiles — see `docs/phases/F1_data_acquisition.md` Known issues) end to end; both audits completed and reported `success`. BRA's land-cover inspection took ~55 minutes this run (up from the ~37-minute figure above, consistent with more real per-tile geometry work now happening, not a regression).
- **IND `data_quality_audit` not yet run — blocked on `parameters.json`, not a data_quality_audit gap (2026-09-24, F2-5).** With IND's `data_acquisition` now `success` (OQ-012 closed, `docs/phases/F1_data_acquisition.md`), an attempt to run `data_quality_audit` for IND correctly fails loud with `CountryParamsRequiredError` (D-core-013, `docs/phases/core.md`): IND has no `config/parameters.json` entry, so the phase's per-technology `slope_threshold_deg` read (`audit.py:269`) has nothing to read. This is the F2-2 objection, still open — `parameters.json`'s `CountryParams` fields require sourced values (capex, OPEX, lifetime, discount rate, terrain slope, biomass yield), none of which exist for IND yet. Not a `data_quality_audit` defect; nothing to fix here. Re-confirmed 2026-09-23 (task F1-2): IND's GWA acquisition itself succeeded (12/12 layers, all hashed), but `data_quality_audit` still fails the same way, so the 12 GWA layers cannot be reported `not_audited`-free for IND specifically — only BRA and PRT actually ran the audit this pass. **Named precisely (task F1-3):** the blocking field is `technologies.<tech>.slope_threshold_deg` (`CountryParams`), and its sourcing for IND is tracked as **OQ-002** in `docs/OPEN_QUESTIONS.md`, Stage R's ownership — not a new question, the existing one. Stated plainly: no Indian raster or vector layer has been checked against a physical sanity range yet; F1b has never actually run for IND at all, on any layer, GWA or otherwise.

## History

- WDPA "[ERROR] found (unreadable)" for PRT (`TopologyException: side location conflict`, first observed 2026-09-21 during a real E3 run) — confirmed pre-existing (reproduced against unmodified commit `e1a57ed` in a worktree, same file). Not a regression, not fixed directly; resolved as a side effect of D-F1b-001's shared-clip-path repair.
