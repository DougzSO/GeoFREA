# core: orchestration, configuration, shared utilities

Status: `in_progress` (A-01, A-02, A-03, A-04 (schema half), A-05, A-06, A-09, A-12, U-05, U-07, Section 9 (`countries.yaml` only) done; A-07 partial (parquet/schema_version/run-ID hash done, raster output is tiled GTiff not true COG); A-08 and A-13 not conformant (see their rows); A-10, A-11 not yet applicable (no phase exercises them yet); A-06 synthetic country fixture runs F1-F2b end to end, no monkeypatch, no network call — see D-core-018, closing OQ-032)
Methodology items: A-01 to A-13, U-01 to U-07, V-01 to V-08, Section 9

## Contract

Provides to all phases: DAG orchestrator, artifact registry and manifest, configuration loading and validation (`parameters.json`, `technologies.yaml`, `countries.yaml`, `experiments.yaml`, `settings.yaml`), geodesy utilities, raster and vector I/O helpers, HTTP retry.

## Conformance

| Item | Requirement | Current state (from `docs/_audit/2026-09_F1-F2b.md` section 8) | Status |
|---|---|---|---|
| A-01 | `requires`/`produces`, topological order, startup validation | `PhaseSpec.requires`/`produces` (`core/orchestrator.py::PhaseSpec`); graph validation and topological sort in `core/orchestrator.py::_validate_and_order()`, called from `Orchestrator.run()`. Tests: `tests/unit/test_orchestrator.py::test_shuffled_spec_list_produces_the_same_order`, `::test_missing_producer_raises`, `::test_dependency_cycle_raises`, `::test_duplicate_producer_raises` | pass |
| A-02 | Artifact registry with paths, hashes, schema versions; upstream loaded from manifest | `ArtifactEntry`/`RunManifest.artifacts` (`core/orchestrator.py`); registration via `PhaseContext.register_artifact()`, hashed (with size/mtime reuse) in `Orchestrator._hash_with_reuse()`, resumed/upstream artifacts checked in `Orchestrator._verify_artifact_integrity()`. F1/F1b/F2b register their whole output as one JSON artifact via `main.py::_register_json_artifact()`; F2a additionally registers one artifact per aligned raster via `main.py::_register_aligned_rasters()` (`aligned/<layer>`). Per-source-file checksums inside F1's `layer_registry` are implemented as `AcquiredLayer.source_sha256` (D-core-016, resolved OQ-024). A resumed phase whose recorded artifact keys or `schema_version` no longer match its current `PhaseSpec` raises `StaleManifestEntryError` (`Orchestrator._check_resume_is_not_stale()`), not a silent mismatch or a raw Pydantic error — real trigger 2026-09-21: `data_quality_audit`'s `audit_report` schema changed shape (`VectorLayerSummary.status` literals), `main.py` bumped its `produces_schema_versions` to `"2.0"`, and resuming the old `"1.0"` entry now raises cleanly. Each successful phase entry also records `consumed_run_ids` (lineage of upstream artifacts it read); a resumed phase whose lineage has moved on logs a warning (`Orchestrator._warn_on_lineage_drift()`) without blocking. Tests: `tests/unit/test_orchestrator.py::test_declared_artifact_not_registered_raises`, `::test_undeclared_artifact_registration_raises`, `::test_upstream_outside_current_run_is_loaded_from_manifest`, `::test_artifact_changed_after_registration_raises_integrity_error`, `::test_deleted_artifact_raises_integrity_error_on_upstream_load`, `::test_hash_reused_when_size_and_mtime_unchanged`, `::test_resume_with_missing_recorded_key_raises_stale_manifest_entry_error`, `::test_resume_with_schema_version_mismatch_raises_stale_manifest_entry_error`, `::test_lineage_is_recorded_for_a_phase_with_requires`, `::test_cross_run_id_resume_warns_without_failing` | pass |
| A-03 | Run targeting | `RunConfig.target_phases`/`rerun_phases` (`core/schemas.py`); resolved into an execution set in `Orchestrator.run()` (transitive closure over `requires`/`produces`); `rerun_phases` re-executes exactly the phases it names and marks their transitive consumers `stale_upstream` — see D-core-012. Tests: `tests/unit/test_schemas.py::test_run_config_accepts_valid_payload`, `::test_run_config_empty_target_phases_raises`; `tests/unit/test_orchestrator.py::test_rerun_phases_reexecutes_only_the_named_phase`, `::test_rerun_phases_marks_downstream_consumers_stale_upstream`, `::test_stale_upstream_phase_is_recomputed_not_resumed`, `::test_rerun_phases_name_outside_needed_closure_raises`, `::test_phase_whose_upstream_is_untouched_still_resumes` | pass |
| A-04 | Technology registry | Schema half: `TechnologyParams` (`core/schemas.py::TechnologyParams`) with `SolarParams`/`WindParams` (biomass removed per S-02) — pass, tests: `tests/unit/test_schemas.py::test_technology_params_accepts_valid_payload`. Config half: `config/technologies.yaml` exists on disk but has **no loader** — `core/config_loader.py` defines exactly four load functions (`load_parameters`, `load_settings`, `load_audit_config`, `load_countries`), none for `technologies.yaml` — so "F5-F7 code contains no technology names" is untested by construction (no phase reads the registry yet) and `data_quality_audit/audit.py` hardcodes its own technology list (`_TECHNOLOGIES`) instead of reading it (see "Unowned findings" below). No per-country technology selection yet. **Second defect in the same file, corrected 2026-09-24 (ADJ-7):** `iec_class_rule` (OQ-005, M-F5-03's per-cell IEC turbine class rule) was placed under `solar:` — IEC turbine classes are a wind concept per M-F5-03 ("Reference turbine power curve per IEC class"); solar has no turbine and no IEC class. It held only a placeholder (`null`, "Pending OQ-005") under solar, never a real rule for either technology — moved to `wind:`, still `null`/pending. Nothing validated this at the time (no loader exists to catch a misplaced field), consistent with this row's `partial` status; no other field in `technologies.yaml` was found under the wrong technology (`docs/_audit/2026-09_embedded_values.md` action 2). | partial |
| A-05 | Country mappings in `config/countries.yaml`; ISO3 literal test | `config/countries.yaml` with BRA/PRT/IND; ISO3 literal scanner in `tests/unit/test_iso3_literals.py` using Python's `tokenize` module to exclude comments/docstrings/strings, runs cleanly on src/ (no violations). `data_acquisition/local_layers.py::_load_countries_config()` and `data_acquisition/fetchers/hydrosheds.py::_get_hydrosheds_region()` both load `config/countries.yaml` directly — no hardcoded country dict remains anywhere in `data_acquisition` (confirmed 2026-09-24, ADJ-3b; corrects `docs/phases/F1_data_acquisition.md`'s A-05 row, which was stale — see that record). Tests: `test_iso3_literals.py::test_no_iso3_literals_in_src` | pass |
| A-06 | Synthetic country fixture | `ZZZ` in `config/countries.yaml` (`synthetic_fixture_root`), every `_LAYER_REGISTRY` layer generated under `GEOFREA_DATA_DIR/fixtures/synthetic_zzz/raw/` (`scripts/generate_zzz_fixture.py`), `parameters.json`/`audit.yaml` entries (test values only, `VerifiedValue.synthetic=True`). F1 through F2b (`suitability_criteria`) all run end to end for ZZZ with **no monkeypatch and no network call** (~12s total, well under the 1-minute target) — see D-core-018's hand-check table. CI job: `.github/workflows/synthetic_fixture.yml`. Also satisfies V-08 (synthetic end-to-end). | pass |
| A-07 | Formats: COG rasters, parquet with schema_version, run ID = hash of config/methodology/commit | `core/orchestrator.py::compute_run_id()` implements the run-ID hash; parquet outputs (where they exist, e.g. `suitability_criteria`) carry a schema version via their Pydantic output models. Raster output (`grid_alignment/raster_alignment.py`, `grid_alignment/vector_alignment.py`, `suitability_criteria/raster_output.py`) writes `"driver": "GTiff"` with `tiled=True`, `blockxsize=256`, `blockysize=256`, `compress="lzw"` — a tiled GeoTIFF, not a true Cloud Optimized GeoTIFF (no overview pyramids built, driver literal is `"GTiff"` not `"COG"`). | partial |
| A-08 | Outputs layout: `raw/<source>/<ISO3\|_global>/`, `interim/<ISO3>/<layer>/`, `outputs/<ISO3>/...` | **Not conformant — see D-core-006.** Every current fetcher (`gadm.py`, `hydrosheds.py`, `wind.py`, `protected_planet.py`) writes fetched raw data to `outputs/<ISO3>/raw/`, not A-08's `raw/<source>/<ISO3\|_global>/`. 4.38 GB of exact-duplicate raw data across two trees as a result (`docs/_audit/2026-09_geofrea_data_inventory.md`). Unresolved: Douglas's verdict needed on correcting the fetchers or revising A-08. | fail |
| A-09 | Failure stops dependents only | `Orchestrator.run()` catches a phase's exception, records `PhaseResult(status="failed")`, and does not raise; transitive dependents get `status="skipped_upstream_failed"`, independent branches still run. `main.py::run_geofrea()` derives a non-zero process exit whenever any target phase ends `"failed"` or `"skipped_upstream_failed"`. Tests: `tests/unit/test_orchestrator.py::test_failure_in_one_branch_does_not_block_an_independent_branch`, `::test_failure_upstream_marks_all_dependents_skipped`; `tests/unit/test_main.py::test_run_geofrea_returns_false_when_a_target_phase_fails`, `::test_run_geofrea_returns_false_when_a_target_phase_is_skipped_upstream_failed`, `::test_run_geofrea_returns_true_when_every_target_phase_succeeds` | pass |
| A-10 | F6/F7 batch processing under `memory.max_batch_gb` | F6 (`lcoe_modeling`) and F7 (`robustness_analysis`) are both `not_started` (`docs/PROGRESS.json`) — no batching code exists yet because neither phase exists yet. | not_started |
| A-11 | Reuse from reference repositories (CRAEI copy-and-adapt, provenance header) | Infrastructure exists (`CRAEI_BASELINE_DIR` env var, `.env.example`, read-only guard intent in `paths.py::ensure_writable()`) but no CRAEI-adapted code exists yet: M-F1-04 (CMIP6) and M-F1-05 (ERA5 gust) — the two methodology items A-11 exists to support — are both `fail` (not implemented). `fetchers/wind.py`'s docstring notes GWA has no CRAEI counterpart and was written fresh, confirming zero A-11 adaptations exist in the codebase today, not merely unaudited ones. | not_started |
| A-12 | Determinism: env vars declared in `.env.example`, loaded at startup, not assumed from shell state | `.env.example` declares all five data-location variables (`GEOFREA_SHARED_RAW_DIR`, `GEOFREA_DATA_DIR`, `GEOFREA_LEGACY_BASELINE_DIR`, `GEOWORLD_BASELINE_DIR`, `CRAEI_BASELINE_DIR`); `main.py` calls `load_dotenv(REPO_ROOT / ".env", override=False)` before any `paths.py` helper runs (D-core-011, resolving D-core-008). Seeds: `compute_run_id()` hashes resolved config + methodology version + git commit, but no run-level RNG seed is recorded yet (no phase that samples exists — F6's Latin hypercube sampler, M-F6-02, is `not_started`). | pass (for the parts exercised today; seed recording itself has nothing to record until F6 exists) |
| A-13 | Traceability: `Implements: M-<item>.` in docstrings of functions implementing a methodology item | Grepped `src/` for `Implements: M-` (2026-09-24, ADJ-3b): **1 hit** (`data_acquisition/fetchers/wind.py::fetch_gwa_product`, `Implements: M-F1-03.`). Dozens of other functions implement a cited M-item (e.g. `reference_grid.py::build_reference_grid` for M-F2a-01, `raster_alignment.py::derive_slope_from_dem` for M-F2a-02) with no such docstring line. CONVENTIONS.md's example (`weibull_capacity_factor`) is itself hypothetical, not yet written. | fail |
| U-01 | Futures: full factorial of climate members and techno-economic samples, both axes preserved in every F6/F7 artifact | F4 and F6 (the phases that would produce members/samples) are both `not_started`. | not_started |
| U-02 | Monte Carlo scope: sampling only within the parameter axis | F6 (the only phase that would sample) is `not_started`. | not_started |
| U-03 | Uncertain parameters declared in `config/experiments.yaml` | `config/experiments.yaml` exists on disk but has **no loader** — same gap as A-04's config half (see "Unowned findings" below). No code reads or validates an uncertain-parameter declaration from anywhere today. | fail |
| U-04 | Sample size convergence protocol | F6 (`not_started`) is where this would be recorded. | not_started |
| U-06 | Land-availability variants (`central`/`strict`/`lenient`) in `config/experiments.yaml` | Depends on U-03's loader, which doesn't exist; F2b/F3 (the phases that would apply variants) are `rework_required`/`not_started`. | not_started |
| U-07 | Evidence tiers 1-3, Tier 3 listed in `docs/LIMITATIONS.md` | `VerifiedValue.tier` (`core/schemas.py`) is implemented and populated throughout `config/parameters.json` (tier 1/2/3 values present for BRA/PRT/IND today); `docs/LIMITATIONS.md` is the designated append point per CLAUDE.md's record-keeping table. | pass |
| U-05 | Parameter schema with `unit`, `tier`, `range` | `VerifiedValue[T]` with full metadata block (unit, source, tier, range, verified, verified_by, verified_date, verification_method, note, status). `ParameterRange` supports uncertain parameters in `experiments.yaml` (H-2, though the file itself is unread today — see U-03). Biomass removed per S-02 (scope: solar, wind only). `capacity_factor` removed per prior correction (2026-09-21). Tests: `tests/unit/test_schemas.py::test_verified_value_accepts_valid_payload`, and 100+ field validation tests | pass |
| V-02 | Analytical tests: CRF/LCOE closed forms, Weibull CF, PVOUT-to-CF, geodesic areas, power-law height interpolation | F5/F6 (`technical_potential`/`lcoe_modeling`), where these closed forms would be implemented and tested, are both `not_started`. | not_started |
| V-03 | Invariants: `R_i,f >= 0`, `0 <= SR <= 1`, `0 <= CF <= 1`, `eligible_area <= cell_area`, capacity sums correctly | F3/F5/F7 (`not_started`) are where these invariants apply; none is implemented. | not_started |
| V-04 | Sanity ranges per resource layer, F1b reports violations, F5 raises on them | F1b's own conformance table (`docs/phases/F1b_data_quality_audit.md`, M-F1b-01 rows) covers the reporting half in full detail — see that record, not duplicated here. F5's raise-on-violation half is `not_started`. | partial (see F1b record for detail) |
| V-05 | Convergence (U-04) | Depends on U-04, `not_started`. | not_started |
| V-06 | No calibration on validation data | Vacuously true today — no parameter-fitting code exists at all (F6/F7 `not_started`); nothing to violate this yet. | not_started |
| V-07 | Scale check: F5-F7 rerun at 0.1 degree | F5/F6/F7 all `not_started`. | not_started |
| V-08 | Synthetic end-to-end (A-06 in CI) | Same evidence as A-06 above — `.github/workflows/synthetic_fixture.yml`, D-core-018. | pass |
| Section 9 | `experiments.yaml`, `technologies.yaml`, `countries.yaml` | `config/countries.yaml`: loaded and used (`load_countries()`, `config_loader.py`) — pass. `config/technologies.yaml` and `config/experiments.yaml`: both exist on disk, syntactically valid, but **neither has a loader anywhere in `src/`** — see A-04/U-03 rows and "Unowned findings" below. | partial (`countries.yaml` only) |

## Active implementation decisions

- **D-core-001 — Technology-level discount rate.** IRENA reports genuinely different discount rates per technology for the same country, so `discount_rate` lives under `parameters.json`'s per-technology entries, never at country level.
  Archive: docs/_archive/2026-09/DECISIONS.md 2026-08-20 - discount_rate architecture fix
- **D-core-002 — Solar and wind parameters from IRENA.** Solar and wind cost/performance parameters (CAPEX, OPEX, lifetime, capacity factor) are populated from IRENA 2024/2025 editions, differentiated per country where the source provides country-level figures.
  Archive: docs/_archive/2026-09/DECISIONS.md 2026-08-20 - solar and wind parameters populated (IRENA 2024/2025)
- **D-core-003 — Single orchestration and audit mechanism, implementing A-01/A-02/A-03/A-09 (verdicts OQ-E2-1 to OQ-E2-4; E4 artifact-granularity closeout).** `PhaseSpec` carries `requires`/`produces`; `Orchestrator.run()` validates the graph (`MissingProducerError`, `DependencyCycleError`, `DuplicateProducerError`) and topologically orders phases (stable tie-break by name) from `RunConfig.target_phases`/`rerun_phases` (see D-core-012 for `rerun_phases`, which replaced `force_rerun` in R-1). `PhaseContext.register_artifact()` feeds `RunManifest.artifacts` (path, sha256, size, mtime, schema_version, phase, run_id), reused when size/mtime match and integrity-checked (missing file or hash mismatch, never recomputed) whenever a phase is resumed *or* loaded as an upstream artifact outside the current run's target set. `RunManifest.schema_version` is pinned `"2.0"` (`LegacyManifestError` otherwise, no migration). A failure records `status="failed"` without raising; dependents get `"skipped_upstream_failed"`; independent branches still run. Artifact granularity per phase: F1 (`data_acquisition`) and F1b (`data_quality_audit`) register one artifact each — a JSON dump of the whole phase output (`layer_registry`, `audit_report`) — because neither phase's output has natural per-item files to hash separately (F1b's report is one document; F1's `AcquiredLayer` entries have no per-layer checksum today, see OQ-024). F2a (`grid_alignment`) registers one artifact per aligned raster field (`aligned/<layer>`, `main.py::_register_aligned_rasters()`) plus the `aligned_rasters` JSON summary, since each raster is independently a real, hashable, resumable file. F2b (`suitability_criteria`) still registers only its own output model as one artifact (see Known issues) — unlike F1/F1b, this is a temporary exception, not the final design, pending the Estágio H rebuild.
  Archive: docs/_archive/2026-09/DECISIONS.md 2026-08-20 - orchestrator + data_quality_audit phase
- **D-core-004 — Slope threshold as a technology parameter.** Terrain slope tolerance is a scientific parameter in `config/parameters.json`, keyed per technology, not a single unsourced country-level fallback — each technology has a physically distinct terrain tolerance. Carried forward into M-F2b-01 E4 (`slope_max_deg[tech]`), same underlying rationale.
  Archive: docs/_archive/2026-09/DECISIONS.md 2026-08-20 - slope_threshold_deg movido para parameters.json

- **D-core-005 — Portable path resolution via `StoredPath` (E5b). Manifest schema version bumped to "2.1".** All data lives in GEOFREA_DATA_DIR (external, never under repository root). `src/geofrea/core/paths.py` is the single module reading data-location environment variables; every other module uses helpers from it (`shared_raw()`, `fetched_raw()`, `interim()`, `phase_dir()`, `manifest_path()`, `log_path()`, `outputs_dir()`). `StoredPath(root, rel)` stores artifact paths with a root identifier ("data", "shared_raw", or "legacy_baseline") + posix-relative path, portable across installations and environments. `ArtifactEntry.path` changed from string to `StoredPath` (manifests written by E5b forward-only; schema "2.0" reads are rejected with `LegacyManifestError`). `ensure_writable(path)` guards against writes to read-only locations (GEOFREA_SHARED_RAW_DIR, GEOFREA_LEGACY_BASELINE_DIR, CRAEI_BASELINE_DIR, GEAR_BASELINE_DIR, GEOWORLD_BASELINE_DIR). Manifests migrated from 2.0→2.1 via `scripts/migrate_manifest_2_0_to_2_1.py` with sha256 re-verification at new location.
  Conformance: A-07, A-08, A-11. Tests: `tests/unit/test_paths.py` (StoredPath resolution, to_stored_path conversion, read-only guards); `tests/unit/test_orchestrator.py` (ArtifactEntry with StoredPath).
  **Revision (2026-09-22, audit FD3b1):** "every other module uses helpers from it" overstates the current state. Call-site census: `shared_raw()`, `fetched_raw()`, `interim()`, `phase_dir()`, `manifest_path()`, `ensure_writable()` — **zero non-test callers each**; `orchestrator.py:451` defines its own separate `manifest_path` property rather than calling `paths.manifest_path()`. Only `log_path()` (`main.py:399`), `outputs_dir()` (`main.py:410`), `to_stored_path()` (`orchestrator.py:673`, `scripts/migrate_manifest_2_0_to_2_1.py:82`), and `StoredPath.resolve()` (`orchestrator.py:538`) are live. The `StoredPath` migration itself is also only half-applied: it covers `ArtifactEntry` (phase-produced artifacts) but not `data_acquisition`'s raw-layer registry (`AcquiredLayer.path`/`.paths`, still plain absolute strings — see `OQ-024`). See D-core-006 to D-core-009 below for the follow-on decisions this revision motivates.

- **D-core-006 — A-08 raw layout is authoritative; fetchers are the non-conformant side (2026-09-22, audit FD3b1).** `METHODOLOGY.md` A-08 (`:337`) specifies `raw/<source>/<ISO3|_global>/` as the layout for fetched raw data. Every current fetcher (`gadm.py:180,206`, `hydrosheds.py:196,219`, `wind.py:40`, `protected_planet.py:73`) instead writes to `outputs/<ISO3>/raw/`, which A-08 does not authorize (`:340` reserves `outputs/<ISO3>/<phase>/<kind>/` for phase outputs, not raw fetch storage). The result is 4.38 GB of exact-duplicate raw data across two trees (`docs/_audit/2026-09_geofrea_data_inventory.md` Actions 1, 2, 7). Current state: unresolved — fetchers have not been corrected, `raw/<source>/<ISO3|_global>/` has not been removed, no file deleted or moved. Douglas's verdict needed on whether to correct the fetchers to match A-08 or to revise A-08 to match the fetchers (a MAJOR-version METHODOLOGY change per Section 0's change protocol if the latter).
- **D-core-007 — `StoredPath` migration status is partial, not complete (2026-09-22, audit FD3b1).** Correcting D-core-005's overclaim: `StoredPath`/`to_stored_path()` cover `ArtifactEntry` (phase-artifact registration, `orchestrator.py:673`) only. `data_acquisition`'s `AcquiredLayer.path`/`.paths` remain plain absolute strings, matching the already-open `OQ-024` (no per-layer checksum). Six of `paths.py`'s ten public helpers (`shared_raw`, `fetched_raw`, `interim`, `phase_dir`, `manifest_path`, `ensure_writable`) have zero callers outside `tests/unit/test_paths.py`. Current state: `paths.py` is on the execution path (via `log_path()`, `outputs_dir()`, `to_stored_path()`, `StoredPath.resolve()`) but most of its surface is unused; no decision yet on whether to wire the unused helpers into their evident intended callers (e.g. `fetched_raw()` into the fetchers, per D-core-006) or remove them.
- **D-core-008 — Reproducibility gap against A-12: no committed path to set the data-location environment variables (2026-09-22, audit FD3b1).** `GEOFREA_DATA_DIR`, `GEOFREA_SHARED_RAW_DIR`, `GEOFREA_LEGACY_BASELINE_DIR` resolve to unset in a fresh process environment, in Windows User/Machine environment variables, in `.env` (which at the time defined `GEOFREA_SHARED_RAW_DIR` under its old name only — not the other two — and was never loaded by any `load_dotenv()` call, confirmed absent from `src/`, `main.py`, and `pyproject.toml`), and in the PowerShell `$PROFILE` (does not exist). The 2026-09-22 BRA/PRT runs necessarily had `GEOFREA_DATA_DIR` set in a since-closed shell session — no file in this repository records how. Current state: `main.py:399,410` calls `paths.py` helpers that raise `MissingPathEnvironmentError` before any phase logic runs, so a fresh clone with only `.env` populated per `.env.example` cannot run any phase. Blocks A-12 (Determinism) entirely, not just non-identical output. Resolved by D-core-011: `.env.example` now covers all four, `main.py` loads `.env` at startup, and `local_layers.py`'s second, independently-named local-database variable was consolidated into `GEOFREA_SHARED_RAW_DIR`.
- **D-core-009 — E5b migration left old-layout directories in place alongside the new ones (2026-09-22, audit FD3b2).** Three directory pairs hold both pre- and post-E5b content: `outputs/<ISO3>/processed/` (old) vs. `interim/<ISO3>/` (new) — same-named `*_clipped.gpkg` files differ in content between the two for both BRA and PRT (regenerated post-migration in one location only); `outputs/<ISO3>/audit/` (old? or new — unclear from content alone) vs. `outputs/<ISO3>/data_quality_audit/reports/` — no filename overlap in either country, meaning a write-location change happened mid-history and neither directory holds the complete run record; `outputs/<ISO3>/grid_alignment/*.tif` vs. `outputs/<ISO3>/grid_alignment/artifacts/*.tif` — fully duplicated, every same-named raster is sha256-identical for both countries. Full detail: `docs/_audit/2026-09_geofrea_data_inventory.md`, "E5b migration residue" section. Current state: none of these directories cleaned up; no file deleted or moved by any audit pass. Douglas's verdict needed on which of each pair is canonical before any cleanup.
- **D-core-010 — GEAR removed as an active reference repository (2026-09-22).** METHODOLOGY A-11/S-08/S-09 and M-F1-04/M-F1-05/M-F4-05 cited GEAR (CRAEI's predecessor) alongside CRAEI as a reuse source; Douglas decided GEAR is discarded entirely — CRAEI is now the sole copy-and-adapt source (METHODOLOGY 1.2.0). `paths.py`'s `GEAR_BASELINE_DIR` read-only guard in `ensure_writable()` and its dedicated test (`test_write_to_gear_raises`) were removed in the same pass; `CRAEI_BASELINE_DIR`'s guard and test (`test_write_to_craei_raises`) are unchanged. `GEAR_BASELINE_DIR` removed from `.env.example`; the local `.env` still defines the name (empty value) — left as-is, outside the scope of a docs/code change.
- **D-core-011 — Environment-variable naming and loading fixed against D-core-008 (2026-09-22).** `main.py` now loads a repository-root `.env` via `python-dotenv` (`override=False`) before any `paths.py` helper can run, and `local_layers.py` resolves its raw-data root through `paths.shared_raw()` instead of a second, independently-named `GEOFREA_RAW_DATA_DIR` variable — closing D-core-008's reproducibility gap against A-12. Every remaining reference to the three retired names (`GEOFREA_RAW_DATA_DIR`, `GEOFREA_PROCESSED_DATA_DIR`, `GEOFREA_OUTPUT_DIR`) outside `docs/_archive/` and `docs/_audit/` has been updated to the current name (`GEOFREA_SHARED_RAW_DIR`).
- **D-core-012 — `force_rerun`'s cascade replaced by explicit `rerun_phases` scope plus manifest-recorded staleness (R-1, 2026-09-23).** Verdict on the draft below (History): `RunConfig.rerun_phases: list[str]` (`schemas.py`) replaces `force_rerun: bool`. `Orchestrator.run()` (`orchestrator.py`) re-executes exactly the phases named in `rerun_phases` — no consumers-graph walk seeds execution scope anymore; `to_attempt` is only `needed` (`target_phases`'s transitive closure over `requires`). A name in `rerun_phases` outside `needed` is a `MissingProducerError`, following the pre-existing `unknown_targets` pattern (same exception type, same validation style, checked immediately after it). After a named phase completes successfully, every phase in its transitive `consumers` closure that currently holds a `"success"` manifest entry is rewritten to `PhaseManifestEntry(status="stale_upstream", invalidated_by=<the rerun phase>, invalidated_in_run=<this run's run_id>)` — this rewrite happens in the same in-memory mutation pass as the rerun phase's own success entry, both persisted by the single `self._write_manifest()` call that follows (`orchestrator.py`, end of the success branch in `run()`): nothing is written to disk until every mutation for that phase's completion — its own entry and every downstream staleness marking — is already applied in memory, so a crash either persists all of it or none of it; there is no intermediate on-disk state where the rerun phase is `"success"` but a dependent it invalidated is still recorded `"success"`. `stale_upstream` is not `"success"`, so the existing resume check (`if not must_force and existing is not None and existing.status == "success"`) already never resumes it — no separate check was needed. A phase holding `stale_upstream` that the current run's `needed` closure requires is therefore recomputed automatically the next time any run needs it, whether or not `rerun_phases` names it that run. **Accepted cost:** invalidation is triggered by the upstream phase having *re-executed*, not by its output having actually *changed* — a rerun that reproduces byte-identical output still marks every downstream consumer `stale_upstream` and forces a real recompute next time, with no cheaper "output was actually identical, skip the recompute" path. See OQ-031 for whether a future revision should compare artifact hashes before marking dependents stale, cross-referencing OQ-026 (the alignment/audit caches this same manifest signal partly answers already). Manifest `schema_version` bumped `"2.1"` → `"2.2"` (`PhaseStatus` gained `"stale_upstream"`; `PhaseManifestEntry` gained `invalidated_by`/`invalidated_in_run`, both optional, defaulting to `None`) — a `"2.1"` manifest on disk is read and its version tag normalized to `"2.2"` in memory before validation (`Orchestrator._load_manifest()`), not migrated by a separate script, since the new fields' `None` defaults make old JSON already valid against the new schema; only pre-`"2.1"` manifests still raise `LegacyManifestError`. `settings.yaml`'s `run.force_rerun` key renamed to `run.rerun_phases` (default `[]`); `config_loader.load_settings()` fails loud with a message naming `rerun_phases` if a settings file still carries `force_rerun`, rather than relying on `RunConfig`'s `extra="forbid"` alone (which would reject it but not say what replaced it).
  Draft superseded below in History.
- **D-core-014 — A phase's exception handling persists artifacts already registered before it raised (F2-4 Part A, 2026-09-24).** `Orchestrator.run()`'s `except Exception` branch now mirrors the success path's artifact-persistence loop (hash, `to_stored_path`, `ArtifactEntry`) for whatever is in `context._artifact_sink` at the point of failure, restricted to keys the `PhaseSpec` actually declares in `produces` (anything else is logged and dropped, so a secondary problem can't mask the real one). Motivated by `data_acquisition`'s per-layer isolation (`docs/phases/F1_data_acquisition.md`): a phase can now register its full output artifact and THEN raise over a subset of failed sub-items, and A-02's registry still records what succeeded even though A-09 still marks the phase `"failed"` and stops dependents. A phase that raises before registering anything (every other phase today) is unaffected — `new_artifacts` is simply empty. Tests: `tests/unit/test_data_acquisition_phase.py::test_partial_layer_failure_still_registers_layer_registry_with_the_failed_layer_named`.
- **D-core-015 — Stale resumed output surfaces as a named error, not a bare ValidationError (F2-4 Part D, 2026-09-24).** `Orchestrator.run()`'s resume branch wraps `spec.output_model.model_validate(existing.output)` and re-raises any `pydantic.ValidationError` as `StaleManifestOutputSchemaError`, naming the manifest path, the phase, and `rerun_phases` as the fix. Distinct from `StaleManifestEntryError` (which catches a `produces`-key or `schema_version` mismatch before ever touching the recorded JSON): this catches drift in the output model's own shape that neither of those checks sees — e.g. a field removed without a `produces_schema_versions` bump (PRT's `grid_alignment` residue, `docs/phases/F2a_grid_alignment.md` — `GridAlignmentResult` dropped `seismic` per S-08/F6-2, but the recorded manifest entry predates that and was never migrated or schema-bumped). Confirmed live against the real PRT manifest. Test: `tests/unit/test_orchestrator.py::test_resume_with_removed_output_field_raises_stale_manifest_output_schema_error`.
- **D-core-013 — `CountryParams` requirement moved to point of use (F2-2 follow-up, 2026-09-23).** `Orchestrator.country_params` and `PhaseContext.country_params` are `CountryParams | None`. `main.py`'s known-country check validates `settings.yaml`'s `run.countries` against `config/countries.yaml` (A-05's mapping registry), not `config/parameters.json`; `run_geofrea()` looks up `parameters.countries.get(country_code)` (`None` if absent) instead of indexing directly. A phase that reads no `CountryParams` field (`data_acquisition`, `grid_alignment`) runs unaffected for a country with no `parameters.json` entry. A phase that does read one calls `PhaseContext.require_country_params(field)`, which raises `CountryParamsRequiredError` naming the country and the field (`data_quality_audit`'s per-technology `slope_threshold_deg`, `main.py::_build_suitability_criteria_inputs`'s `criteria`) — caught by `Orchestrator.run()`'s existing per-phase exception handling (A-09) and recorded as `PhaseResult(status="failed")`, same as any other phase failure, never raised out of `Orchestrator.run()`. `CountryParams`'s own fields stay non-nullable for a country that has an entry — this only removes the requirement that every run-targeted country have one before any phase executes. Does not change A-03's stated text: `run.countries` still names which countries to run; what changed is which config file defines "known" and when (not whether) `CountryParams` is required. Tests: `tests/unit/test_main.py::test_run_geofrea_succeeds_for_country_absent_from_parameters_json`, `::test_audit_run_fails_loud_naming_country_and_field_when_params_absent`, `::test_orchestrator_records_audit_as_failed_not_raised_when_params_absent`, `::test_build_suitability_criteria_inputs_fails_loud_when_params_absent`, `::test_run_geofrea_still_succeeds_for_bra_and_prt`.

- **D-core-016 — OQ-024 resolved: `AcquiredLayer` gains `source_sha256`, computed at fetch time, for every layer (2026-09-23).** Verdict: a per-file sha256 (`AcquiredLayer.source_sha256: dict[str, str] | None`, keyed by path string — one entry per file, so a multi-file layer like `land_cover` carries one hash per tile) is computed inside `run_acquisition_phase()`'s per-layer resolution loop (`data_acquisition/phase.py`, `_hash_layer_files()`), right after that layer's path/paths resolve successfully — not deferred to a separate pass. Measured first (F1-1b action 1): hashing every layer already resolved for BRA and PRT, per layer — worst case BRA `land_cover` (112 tiles, 6.2 GB) at 55.8s and BRA `population` (4.2 GB) at 30.2s, both well under a minute; every other layer under 17s. Null-for-cost rule: a layer whose hashing exceeds `_HASH_BUDGET_S` (60.0s) stores `source_sha256=None` with `source_sha256_skipped_reason` naming the cause, rather than being dropped from the model or blocking resolution — no layer hits this today, but the mechanism exists because the budget is a real ceiling, not a value picked to always pass. The hash is a fact about the file, never a gate: nothing in `AcquiredLayer` or `phase.py` compares a freshly computed hash against a prior one. The one place a checksum mismatch already fails loud is METHODOLOGY M-F1-07 (GADM local-first), which uses its own dedicated, unrelated mechanism — `fetchers/gadm.py:68` (`GadmChecksumMismatchError`) raised at `fetchers/gadm.py:146-151` when a local shapefile's sha256 disagrees with `countries.yaml`'s `gadm_level0_sha256` — not `source_sha256`. `layer_registry`'s artifact schema version bumped `"1.0"` → `"1.1"` (`main.py::_LAYER_REGISTRY_SCHEMA_VERSION`, now declared in `data_acquisition`'s `PhaseSpec.produces_schema_versions`), following the same forward-compatible pattern as `_AUDIT_REPORT_SCHEMA_VERSION`: both new fields default to `None`, so an existing `"1.0"`-recorded `layer_registry.json` for BRA, PRT or IND still loads (`AcquisitionResult.model_validate()` succeeds, hashes read back as `None`) — the version bump only blocks *resuming* `data_acquisition` itself against a stale `"1.0"` entry (`StaleManifestEntryError`), forcing a real rerun before new hashes appear, rather than silently reporting `None` forever. Tests: `tests/unit/test_data_acquisition_phase.py::test_run_acquisition_phase_records_source_sha256_at_resolve_time`, `::test_run_acquisition_phase_skips_hash_over_budget_and_records_reason`, `::test_acquired_layer_missing_source_sha256_defaults_to_none_on_load`.

- **D-core-018 — OQ-032 resolved: ZZZ runs F1-F2b end to end, no monkeypatch, no network call; test-value/sourced-value separation enforced by test (playbook COMMAND ADJ-3, 2026-09-24).** ISO3 `ZZZ` (deliberately outside any real ISO 3166-1 alpha-3 assignment) registered in `config/countries.yaml` with `synthetic_fixture_root: "fixtures/synthetic_zzz/raw"` (relative to `GEOFREA_DATA_DIR`, generated by `scripts/generate_zzz_fixture.py`) — never under `GEOFREA_SHARED_RAW_DIR`, which stays real-country-only and read-only. Extent: lon `[20.00, 20.30]`, lat `[-5.00, -4.80]` — 6 x 4 decision cells (0.05 deg) = 30 x 20 pixels (0.01 deg), non-square so row/column snapping are tested independently.
  **Verdict on OQ-032 (Douglas):** a synthetic country carries test values, not sourced values. A test value is chosen for what it exercises; its justification is that choice, and it never reaches a thesis output. METHODOLOGY U-07's sourcing rule governs values that enter a result — it does not govern a fixture. What must be enforced is that the two never mix. Implemented as: (1) `VerifiedValue` (`core/schemas.py`) gained a `synthetic: bool = False` field with a validator forbidding `synthetic=True` together with a real `tier` (1/2/3) — a test value's tier slot is never a real evidence tier, per the verdict's own wording. Every one of ZZZ's `parameters.json` entries (`technologies.{solar,wind}.*`, `criteria.yield_by_land_cover`, `criteria.terrain_slope_threshold_deg`) sets `synthetic=True`, `tier=null`, `source=null` — chosen for what each exercises (see each field's own `note`, e.g. `criteria.terrain_slope_threshold_deg=10.0` chosen against the fixture DEM's known uniform slope gradient, `technologies.*.discount_rate`/`lifetime_years` chosen for a clean capital-recovery-factor hand-check), never a real cost/technical estimate. (2) Separation enforced by test, not convention: `tests/unit/test_synthetic_value_separation.py`'s four checks — no synthetic country in `main()`'s default thesis-scope country expansion (`main.py::main()` now filters `countries.yaml`'s `synthetic_fixture_root` out of the empty-`run.countries` default), no real country's `parameters.json` value marked `synthetic=True`, no real country's `countries.yaml` mapping carries `synthetic_fixture_root`, every ZZZ value IS marked `synthetic=True`.
  **Resolving-without-monkeypatch (Action 1):** `phase.py`'s per-layer dispatch (`run_acquisition_phase()`, ~L515-524) chooses `fetch_handler` for a "fetched"-provenance layer (`borders`/`admin1`/`wind`+11 GWA/`protected`/`lakes`/`rivers`/`power_plants`) or `local_handler` for a "local_only" one, looked up from two disjoint, country-blind dicts (`_FETCHED_LAYER_HANDLERS`/`_LOCAL_PATH_HANDLERS`) keyed only by `layer_name`. ZZZ takes the local path instead because a new check, `local_layers.resolve_synthetic_fetched_layer(country_code, layer_name)`, runs first and returns a real path whenever `countries.yaml[country_code]["synthetic_fixture_root"]` is set (only ZZZ) AND the layer is in a new relpath map (`_SYNTHETIC_FETCHED_LAYER_RELPATHS`) — for every real country this returns `None` immediately (no such key) and the dispatch is byte-for-byte unchanged. The 6 already-`local_only` layers (`elevation`/`population`/`grid`/`roads`/`solar`/`land_cover`) needed a smaller change: `local_layers.py`'s six resolvers now read their root via a new `_country_raw_root(country_code)` (falls back to the unchanged `_raw_data_dir()`/`GEOFREA_SHARED_RAW_DIR` for every real country) instead of calling `_raw_data_dir()` directly — no resolver's own path-construction logic changed. No layer needed a resolver's internal logic rewritten; nothing was reported as "cannot be local_only without changing a resolver."
  **Run result (no monkeypatch, no network call, `socket.socket.connect`/`create_connection` patched to raise if attempted — none did):** F1+F1b in 0.93s, +F2a in 0.46s, +F2b in 10.15s (additive total ~11.5s), all four phases `status="success"` — well under the 1-minute completion target. Hand-check table:
  | Check | Predicted | Observed | Agrees? |
  |---|---|---|---|
  | Elevation range | 0-290 m | 0-290 m | yes |
  | Population range | 0.0-380.0 | 0.0-380.0 | yes |
  | Power plants total capacity | 100 MW | 100 MW | yes |
  | Power plants by fuel | Solar 70, Wind 30 | Solar 70, Wind 30 | yes |
  | F1b consistency alerts | 0 (once `audit.yaml`'s `country_overrides.ZZZ` matches the fixture's own generated resolutions, Action 9) | 0 | yes |
  | Grid nesting (G-3) | 30x20 px, bounds exactly `[20.00, -5.00, 20.30, -4.80]` (already exact 0.05 deg multiples) | **22x30 px, bounds `[20.000, -5.010, 20.300, -4.790]`** | **no — see finding below** |
  | `terrain_score` mean (F2b) | ~0.9483 (`clip(1 - 0.5166/10, 0, 1)`, slope-only) | 0.794 | **no — see finding below** |
  | `solar_resource`/`wind_resource` mean (F2b) | not predicted in advance | 0.500 both | see note below |
  **Finding 1 (grid nesting/origin, directly reproducing what G-3 must fix):** `reference_grid.py::build_reference_grid()` (L58-61) snaps bounds via `np.floor(minx/resolution_deg)`/`np.ceil(maxy/resolution_deg)` with no epsilon tolerance. ZZZ's raw border bounds are exactly `[20.0, -5.0, 20.3, -4.8]` (confirmed reading the generated shapefile back), which floor/ceil cleanly at `resolution_deg=0.01`. But `grid_alignment/adapter.py`'s country boundary goes through `core/geo_utils.py::get_mainland_gdf()` (L285-295) first, which reprojects to a local UTM CRS and back to EPSG:4326 (`gdf.to_crs(utm_crs)...to_crs("EPSG:4326")`) to pick the largest polygon — a reprojection round-trip that is not bit-exact even for a perfect rectangle. Reproduced directly: after this round-trip, `miny/0.01 = -500.0000000000006` (floors to -501, i.e. -5.01) and `maxy/0.01 = -479.9999999999994` (ceils to -479, i.e. -4.79) — both x-axis divisions happened to land exactly on integers this run and were unaffected, but that is latitude-dependent luck, not a guarantee. This is the mechanism: any country boundary whose extent falls exactly on a resolution-degree multiple (ZZZ's does, by design) is vulnerable to `get_mainland_gdf()`'s UTM round-trip perturbing it by ~1e-13 deg, enough to flip `floor`/`ceil` a full pixel-bucket at that exact boundary, silently padding the grid by up to one pixel per edge and breaking the "cells nest exactly" guarantee S-06/M-F2a-01 require. Not fixed here — playbook task G-3 owns the fix (candidates: round bounds to a fixed decimal precision before dividing, or compare against `resolution_deg` with an epsilon tolerance in `build_reference_grid()`); this is the reproduction the fix should be tested against.
  **Finding 2 (`terrain_score` disagreement — an incomplete a-priori prediction, not a fixture defect):** the prediction above assumed `terrain_score = clip(1 - slope/threshold, 0, 1)` alone. The real formula (`suitability_criteria/criteria_functions.py::compute_terrain_score()`, ~L326-327) is `w_slope * slope_score + w_tri * tri_score`, with `w_slope=0.6`/`w_tri=0.4` from `parameters.json`'s global `criteria.terrain_slope_weight`/`terrain_tri_weight` — a terrain-ruggedness-index term this prediction omitted entirely. The observed mean (0.794) is consistent with that decomposition (`0.6*0.9483=0.569`, implying a TRI-score mean of `~0.5625` to reach 0.794) but was not independently rederived from TRI's own formula — reported as a disagreement per instruction, not silently reconciled.
  **Note (degenerate uniform-input normalization):** `solar_resource`/`wind_resource` both report mean=0.500 exactly. ZZZ's PVOUT and wind-speed rasters are uniform constants by design (Action 2's "known constant" choice) — a min-max normalizer given a zero-variance input has no real range to scale against, and the code's degenerate-case behavior returns the midpoint 0.5 uniformly rather than erroring. Expected once traced, not predicted in advance.
  **CI (Action 7):** `.github/workflows/synthetic_fixture.yml`, alongside (not replacing) `regression.yml` — generates the fixture fresh every run (no download, no external asset), runs F1-F2b for ZZZ with a 60s ceiling, then the four separation tests.
  **Config additions:** `config/audit.yaml` gained `country_overrides.ZZZ` (Action 9) — `AuditConfig.country_overrides: dict[str, dict[str, AuditLayerConfig]]` (new field, `data_quality_audit/schemas.py`), consulted in `audit.py::run_audit_phase()` right after the global `expected_resolutions` dict is built, replacing (not merging into) any matching key for the current country only — a no-op for every real country (empty dict). Without it, ZZZ's fixture rasters (generated at 0.01/0.0025 deg, not the real products' native resolutions) would have shown spurious resolution-mismatch alerts against real-product expectations, not `not_audited`.

## Known issues

- **`config/technologies.yaml` and `config/experiments.yaml` have no loader (found 2026-09-24, ADJ-3; recorded here 2026-09-24, ADJ-5).** `core/config_loader.py` defines exactly four load functions (`load_parameters`, `load_settings`, `load_audit_config`, `load_countries`) — neither `technologies.yaml` nor `experiments.yaml` is read by any code path in `src/`. `data_quality_audit/audit.py` hardcodes its own `_TECHNOLOGIES` tuple rather than reading the technology registry A-04 specifies, and no uncertain-parameter declaration from U-03 is read from anywhere since `experiments.yaml` is unread. This is A-04 and U-03 unmet in a phase (`data_quality_audit`) that is already `built_pending_conformance`, not merely a not-yet-reached future milestone. Owner: whichever milestone first needs each file — `technologies.yaml` by A-04's first real consumer (MS-8/MS-9, F5/F6), `experiments.yaml` by U-01/U-03's first real consumer (MS-7/MS-9, F4/F6).
- **The default country-run expansion still derives from `config/parameters.json`, not `config/countries.yaml` — the remaining half of what D-core-013/F2-3 started (found 2026-09-24, ADJ-3; recorded here 2026-09-24, ADJ-5).** `main.py:511-514` (`default_countries = [c for c in parameters.countries if not countries_config.get(c, {}).get("synthetic_fixture_root")]`) iterates `parameters.countries`, not `countries_config`. A country registered in `countries.yaml` alone (no `parameters.json` entry) runs a phase that reads no `CountryParams` field only when named explicitly in `settings.yaml`'s `run.countries` (D-core-013's mechanism, confirmed live via `main.py:463`'s `parameters.countries.get(country_code)` returning `None` safely) — never via the default expansion when `run.countries` is left empty. D-core-013 removed the requirement that a named country have a `parameters.json` entry before running a parameter-free phase, but did not touch which countries are *automatically* selected when `run.countries` is empty. Confirmed pre-existing since the first orchestrator commit (`c4c4c75`), not introduced by ADJ-2b's synthetic-country filter (`docs/_audit/2026-09_repo_cleanup.md` action 5; `git log -L` on this block). Owner: unassigned — no milestone currently owns reconciling the default-expansion path with the explicit-listing path's more permissive behavior.

- **`build_reference_grid()`'s floor/ceil snapping is not robust to floating-point noise at exact grid-boundary extents (found via the ZZZ fixture, D-core-018, playbook task G-3 to fix).** `reference_grid.py:58-61` divides by `resolution_deg` and calls `np.floor`/`np.ceil` with no epsilon tolerance; `core/geo_utils.py::get_mainland_gdf()`'s UTM reprojection round-trip (L285-295) can perturb an exactly-on-boundary extent by ~1e-13 deg, enough to flip floor/ceil a full pixel-bucket and silently pad the grid by up to one pixel per edge — reproduced on ZZZ (predicted 30x20 px, observed 22x30 px). Breaks the "0.05 deg cells nest exactly" guarantee (S-06/M-F2a-01) whenever a country's extent happens to land exactly on a resolution multiple. Not fixed here.

- **Deferred to playbook task G-3 (Douglas, 2026-09-21):** Remove duplicated Pydantic schema defaults in adaptive resolution mode fields (7 fields total): `AdaptiveResolutionConfig.target_pixels` (src/geofrea/core/schemas.py:615), `AdaptiveResolutionConfig.min_deg` (src/geofrea/core/schemas.py:616), `AdaptiveResolutionConfig.max_deg` (src/geofrea/core/schemas.py:617), `ResolutionsConfig.suitability` (src/geofrea/core/schemas.py:642), `ResolutionsConfig.adaptive` (src/geofrea/core/schemas.py:643), `GeospatialConfig.resolutions` (src/geofrea/core/schemas.py:655), `SettingsFile.geospatial` (src/geofrea/core/schemas.py:675). Fields will become absent instead of required after removal.
- Five `criteria.*` parameters are validated but never read.
- F2b (`suitability_criteria`) registers only one artifact for its whole output model (`suitability_criteria_result`), not a per-layer breakdown — temporary, pending the Estágio H rebuild of F2b into `siting_layers` (exclusion/cost-driver/resource layers, METHODOLOGY M-F2b-01 to M-F2b-05).
- F2a's `produces` hardcodes 11 raster keys (`main.py::_ALIGNED_RASTER_LAYER_KEYS`, excludes only `seismic`, out of scope per S-08). This holds for PRT and BRA today (every field resolves to a real raster). A future country legitimately missing one of these 11 inputs (e.g. IND, OQ-012) — or a country with zero existing plants, leaving `GridAlignmentResult.plants` `None` — would raise `UndeclaredArtifactMissingError` at `grid_alignment`, not a graceful partial result. Revisit when IND is wired (MS-2).
- `StaleManifestEntryError`/`consumed_run_ids` lineage only cover phases registered through `main.py::_build_phase_specs()` today (F1/F1b/F2a/F2b) — no other phase declares `produces_schema_versions` yet, so a future phase's own breaking output-schema change needs the same manual bump `data_quality_audit` got (see docs/phases/F1b_data_quality_audit.md D-F1b-001), not an automatic detection.

## Shared geometry repair (A-02, moved from suitability_criteria 2026-09-21)

`core/geo_utils.py::repair_invalid_geometries()` / `GeometryRepairReport` — every `clip_vector_to_country()`/`read_clipped_to_country()` caller now repairs invalid geometries unconditionally (`shapely.make_valid(method="structure")`), including a post-simplify validity check on the country polygon itself. Previously `suitability_criteria`-only (WDPA); see docs/phases/F2b_siting_layers.md D-F2b-004 and docs/phases/F1b_data_quality_audit.md D-F1b-001 for the two real consumers. V-01 regression (`protected_areas`, PRT+BRA) confirmed max abs diff = 0.0 against the pre-refactor implementation.

## History

- D-core-017 ("Synthetic country fixture generated for A-06/V-08", playbook COMMAND ADJ-2, 2026-09-24: fixture generation + F1-only run via monkeypatched fetch handlers, F1b blocked on OQ-032) — superseded by D-core-018 above (COMMAND ADJ-3, same day): OQ-032 resolved, ZZZ now runs F1-F2b with no monkeypatch and no network call.
- D-core-005 (E2 draft design for A-01/A-02/A-03/A-09, "awaiting verdict") — approved (OQ-E2-1 to OQ-E2-4) and implemented; superseded by D-core-003's updated text above.
- "Draft decision — force_rerun semantics (awaiting verdict, 2026-09-22)" — approved and implemented as D-core-012 (R-1, 2026-09-23). The draft's two-field split (`rerun_phases` exact vs. `rerun_phases_cascade` downstream) was not the verdict actually implemented: the approved design keeps only one field (`rerun_phases`, exact re-execute) and replaces the cascade option with automatic manifest-recorded `stale_upstream` marking of consumers, recomputed lazily by whichever later run actually needs them — never re-executed eagerly in the same run that invalidated them.
- WDPA "[ERROR] found (unreadable)" for PRT (`TopologyException: side location conflict`, first observed 2026-09-21 during a real F1/F1b/F2a run) — confirmed pre-existing (reproduced against unmodified commit `e1a57ed` in a worktree, same data). Resolved as a side effect of moving geometry repair into the shared clip path (see docs/phases/F1b_data_quality_audit.md D-F1b-001): a real force_rerun of `data_quality_audit` for PRT (2026-09-21) now reports `protected: [OK] found`, 14/184 WDPA features repaired.
