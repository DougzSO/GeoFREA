# F2a grid_alignment

Status: `built_pending_conformance`
Methodology items: M-F2a-01 to M-F2a-04, V-01

## Contract

Requires: `acquisition_registry`. Produces: `aligned_layers` (COGs on the 0.01 degree grid, snapped for 0.05 degree nesting), distance rasters with `distance_capped` flags.

## Conformance

| Item | Requirement | Current state | Status |
|---|---|---|---|
| M-F2a-01 | 0.01 degree grid snapped for exact 5 x 5 nesting | Fixed 0.01 degree confirmed; `grid_alignment/reference_grid.py:build_reference_grid` (lines 56-64) snaps bounds only to 0.01 degree multiples (`floor`/`ceil` by `resolution_deg`), never to 0.05 degree multiples, and does not assert `width`/`height` are multiples of 5 | fail |
| M-F2a-02 | Geodesic distances, areas, and slope | Geodesic except `derive_slope_from_dem` (`KM_PER_DEG_LAT = 111.32`) | fail |
| M-F2a-03 | Distance cap as parameter with flag | `LINEAR_FEATURE_MAX_DIST_KM = 100.0` hardcoded, no flag | fail |
| M-F2a-04 | Per-height wind alignment, no cross-height combination | AHP combination code present (`WIND_AHP_MATRIX`) | fail |
| V-01 | Frozen parity for aligned rasters | Legacy-baseline parity; fixtures to refreeze after M-F2a-02 | fail |
| CONVENTIONS | no duplicated defaults | Pydantic schema defaults duplicate settings.yaml values in ResolutionsConfig/AdaptiveResolutionConfig/GeospatialConfig/SettingsFile | fail |

## Active implementation decisions

- **D-F2a-001 — Legacy grid_alignment ported with targeted revisions.** `grid_alignment` preserves the legacy's validated scientific logic where applicable, with specific, individually justified revisions (geodesy centralization, resolution fixed, distance cap) rather than a full rewrite or an unreviewed port.
  Archive: docs/_archive/2026-09/DECISIONS.md 2026-09-08 - grid_alignment (Fase 2a) portado do legado
- **D-F2a-002 — Bowring centralization, unified distance cap, fixed resolution.** Bowring geodesic calculations are centralized in `core/geodesy.py`; grid/road/river distance search uses one unified cap rather than a different value per feature (to be exposed as `distance_cap_km` per M-F2a-03, not the current hardcoded constant). The 0.01 degree fixed (non-adaptive) resolution is implemented; snapping bounds so 0.05 degree cells nest exactly (5 x 5 pixels) is not implemented — this decision is **partially conformant** with M-F2a-01 as written (see the M-F2a-01 conformance row and the TODO under Known issues). The fourth verdict of this same entry (retaining the wind AHP combination matrix) is retired — see `docs/_audit/2026-09_records.md` migration report, invalidated by M-F2a-04.
  Archive: docs/_archive/2026-09/DECISIONS.md 2026-09-09 - Passo 4: os 4 veredictos metodológicos de grid_alignment
- **D-F2a-003 — Fail-loud on corrupted land-cover tiles.** `mosaic_land_cover` fails loudly when a tile overlapping the country is corrupted, rather than silently absorbing an integrity failure of a file that is present.
  Archive: docs/_archive/2026-09/DECISIONS.md 2026-09-11 - grid_alignment: mosaic_land_cover fail-loud em tile corrompido

## Known issues

- **Known issue (CONVENTIONS, fail — see conformance row):** Adaptive resolution mode and its defaults (target_pixels, min_deg, adaptive_pixel_ceiling_deg, suitability) are removed in playbook task G-3; fields become absent, not required. Deferred by Douglas on 2026-09-21. (Field renamed from `max_deg` 2026-09-22 to avoid confusion with S-06's unrelated 0.05deg decision cell — see next entry.)
- Adaptive resolution mode exists as opt-in; not used by the method.
- **TODO (M-F2a-01, fail — see conformance row, evidence `grid_alignment/reference_grid.py:build_reference_grid` lines 58-61):** `build_reference_grid()` snaps `minx`/`miny`/`maxx`/`maxy` to multiples of the 0.01 degree pixel resolution only (`np.floor(minx / resolution_deg) * resolution_deg` with `resolution_deg = 0.01`); it never snaps to 0.05 degree multiples, so M-F2a-01's "0.05 degree cells nest exactly on 5 x 5 pixels" invariant is not currently satisfied. Empirically confirmed broken on 2026-09-22 against the real GADM polygons on disk, not just read from the code: PRT's snapped grid origin (-31.27, 30.03) is offset from the nearest 0.05deg boundary by 0.03deg (3 pixels) in both x and y; BRA's snapped origin (-73.99, -33.75) is 0.05deg-aligned in y only by coincidence, still offset by 0.01deg (1 pixel) in x. Neither country's current grid actually nests. This has not caused a visible failure only because F3 (`land_eligibility`, M-F3-03's 5x5 aggregation) is not yet implemented — it blocks M-F3-03 from being exact once F3 exists. Fix: snap bounds to 0.05 degree multiples before generating the 0.01 degree grid. Add a test asserting `width % 5 == 0 and height % 5 == 0` for the resulting `GridContext`.

- **Known issue (memory, found 2026-09-22, force_rerun BRA/PRT run).** `reproject_to_grid()` (`src/geofrea/grid_alignment/raster_alignment.py:95`, `src_array = src.read(1)`) reads the entire source raster into memory in one call. For BRA's `population` layer this allocated a `(1, 46814, 54172)` float32 array (9.45 GiB) and raised `numpy._core._exceptions._ArrayMemoryError: Unable to allocate 9.45 GiB for an array with shape (1, 46814, 54172) and data type float32`, failing the whole `grid_alignment` phase for BRA. No fix proposed here; assigned to Stage G.
- **Known issue (A-08 conformance, found 2026-09-22).** Phase outputs are written directly to `outputs/<ISO3>/<phase>/` (`src/geofrea/grid_alignment/alignment.py:270`, `out_dir = context.outputs_dir / context.country_code / "grid_alignment"`) rather than `outputs/<ISO3>/<phase>/<kind>/` as METHODOLOGY A-08 specifies. Assigned to G-3, alongside the M-F2a-01 snapping fix.
- **PRT's current `grid_alignment` output is provisional (2026-09-22).** Produced by an unrequested force_rerun cascade (see `docs/phases/core.md`'s decision on the FD4c/FD4d/FD5 pass) rather than a deliberate F2a conformance run; overwritten once Stage G lands.
- **PRT's on-disk `grid_alignment` manifest entry still carries the removed `seismic` field — failure made legible, not fixed (2026-09-24, F2-4 Part D, owner G-3).** `Orchestrator.run()`'s resume path (`orchestrator.py`, the `spec.output_model.model_validate(existing.output)` call inside the `existing.status == "success"` branch) now wraps that call and re-raises any `pydantic.ValidationError` as `StaleManifestOutputSchemaError`, naming the manifest path, the phase, and pointing at `rerun_phases` as the way past it — confirmed live against the real PRT manifest (`python main.py` with default settings now raises `StaleManifestOutputSchemaError: Manifest .../outputs/PRT/manifest.json has a recorded output for phase 'grid_alignment' that no longer validates ... seismic ... Name 'grid_alignment' in rerun_phases to discard the stale entry and recompute it instead of resuming.` instead of a bare `ValidationError`). The manifest data itself is untouched — not migrated, per instruction: G-3 rewrites F2a and is the right point to regenerate PRT's `grid_alignment` output cleanly. Test: `tests/unit/test_orchestrator.py::test_resume_with_removed_output_field_raises_stale_manifest_output_schema_error`.
- **Prior finding, superseded by the entry above (kept for context — this is F-6 residue, not a new finding).** `GridAlignmentResult` (`src/geofrea/grid_alignment/schemas.py`) has had no `seismic` field since S-08's removal (F6-2); a repo-wide grep for `seismic` in `src/`, `tests/`, `config/` and `docs/` (excluding `_archive/` and `_audit/`) returns 0 hits, confirming the code side of F-6's completion criterion genuinely held. The residue is data, not code: `GEOFREA_DATA_DIR/outputs/PRT/manifest.json`'s `phases.grid_alignment.output` JSON blob was frozen by a run that predates the schema change (see the "provisional" note above — the same unrequested force_rerun cascade), and Orchestrator.run()'s resume path (`spec.output_model.model_validate(existing.output)`, `orchestrator.py:706`) re-validates that stale blob against the *current* `GridAlignmentResult` (`extra="forbid"`) on every resume attempt, raising `pydantic.ValidationError: 1 validation error for GridAlignmentResult / seismic / Extra inputs are not permitted`. This is why F-6's grep-based completion criterion passed while a real resume of PRT still fails today: the criterion checked the code, and the code is clean; nothing in F-6 re-validated or rewrote already-recorded manifest data against the new schema. Not fixed here — out of this task's scope (`src/`, `tests/`, `config/` only) and the PRT/BRA manifests are untouched by F2-2's IND-only run. Fix (G-3 or whenever PRT's `grid_alignment` is next legitimately rerun): delete or rerun the stale `grid_alignment` manifest entry so it revalidates clean.
- **slope's expected-resolution expectation is now unowned (2026-09-23, see `docs/phases/F1b_data_quality_audit.md` D-F1b-004).** F1b removed its `slope` entry from `config/audit.yaml` on the grounds that slope isn't an F1b layer — it's produced here, by `derive_slope_from_dem()` (`raster_alignment.py`). No equivalent expectation (native/target resolution, sanity range) exists in this phase's own configuration today. G-1, when it reworks the slope derivation (see M-F2a-02's `fail` row above), should decide whether F2a needs a documented resolution expectation for its own derived slope raster, and where it would live if so.
- **Slope: derived here from the DEM, not an acquired layer — confirmed 2026-09-24 (ADJ-1/ADJ-2), recorded once so the question is not reopened.** Slope has no file to audit at F1/F1b; the pipeline's slope comes from this phase, `raster_alignment.py::derive_slope_from_dem()`. M-F2a-02 requires geodesic derivation for all distances, areas, and slope. The current implementation uses a constant `KM_PER_DEG_LAT = 111.32` degrees-to-km factor (see the M-F2a-02 conformance row, `fail`) instead of `core/geodesy.py`'s latitude-dependent Bowring-series scale factors — G-1 owns replacing it. **Confirmed on disk (2026-09-24): `GEOFREA_SHARED_RAW_DIR/elevation/<dir>/<ISO3>_slope.tif` pre-derived slope files do exist for all 7 countries in the local database, including BRA, PRT and IND — but `local_layers.py` has no resolver for them (only `resolve_elevation_path()` exists in that module) and no `_LAYER_REGISTRY`/`_LOCAL_PATH_HANDLERS` entry in `data_acquisition/phase.py` references them. They are not what the pipeline consumes: `derive_slope_from_dem()` is the only slope path GeoFREA actually runs, computed fresh from the Copernicus DEM each time, not read from these pre-derived files. G-1's geodesic fix belongs in `derive_slope_from_dem()`; the pre-derived files remain unused and out of scope for that fix (their own unit — degrees vs. percent — was never checked, since GeoFREA does not read them).

## History

None.
